FROM node:20-slim

WORKDIR /app

COPY package.json ./
RUN corepack enable && corepack prepare pnpm@9.12.0 --activate \
  && pnpm install --frozen-lockfile=false

COPY . .

EXPOSE 3000
CMD ["pnpm", "dev"]
