# GitHub Copilot Instructions

This project centralizes all agent guidance in `AGENTS.md` at the repository root. Read it before making any changes.

Project standards live in `.ai/standards/`, grouped by context (common, backend, frontend). Always consult the relevant standards for your context before writing or modifying code. See the "Project standards" section in `AGENTS.md` for the full mapping.
