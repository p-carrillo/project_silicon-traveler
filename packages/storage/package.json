{
  "name": "@silicon-traveler/storage",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "pnpm -w vitest -c vitest.workspace.ts run --project storage",
    "test:unit": "pnpm -w vitest -c vitest.workspace.ts run --project storage --dir test/unit",
    "test:integration": "pnpm -w vitest -c vitest.workspace.ts run --project storage --dir test/integration"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
