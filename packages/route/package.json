{
  "name": "@silicon-traveler/route",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "pnpm -w vitest -c vitest.workspace.ts run --project route",
    "test:unit": "pnpm -w vitest -c vitest.workspace.ts run --project route --dir test/unit",
    "test:integration": "pnpm -w vitest -c vitest.workspace.ts run --project route --dir test/integration"
  },
  "dependencies": {
    "@silicon-traveler/shared": "workspace:*",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
