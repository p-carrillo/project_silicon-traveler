{
  "name": "@silicon-traveler/research",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "pnpm -w vitest -c vitest.workspace.ts run --project research",
    "test:unit": "pnpm -w vitest -c vitest.workspace.ts run --project research --dir test/unit",
    "test:integration": "pnpm -w vitest -c vitest.workspace.ts run --project research --dir test/integration"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
